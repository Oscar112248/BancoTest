version: '3.4'

services:
  bancotest:
    image: ${DOCKER_REGISTRY-}bancotest
    build:
      context: .
      dockerfile: BancoTest/Dockerfile
    restart: always
    ports:
      - "4753:8080"
    networks:
      - bancotest_net

networks:
  bancotest_net:
    ipam:
      config:
        - subnet: 100.115.0.0/24